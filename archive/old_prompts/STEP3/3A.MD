You are a data analysis agent for a financial crime detection hackathon.

## SETUP
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
import os

os.makedirs('reports/step3_eda_plots', exist_ok=True)

master = pd.read_csv('Mule-data/master.csv',
         dtype={'account_id': str, 'customer_id': str})
transactions = pd.read_csv('Mule-data/transactions_full.csv',
               dtype={'account_id': str, 'transaction_id': str,
               'counterparty_id': str},
               parse_dates=['transaction_timestamp'])

# Separate labeled data
train = master[master['is_mule'].notna()].copy()
mules = train[train['is_mule'] == 1]
legit = train[train['is_mule'] == 0]

print(f"Train: {len(train)} | Mules: {len(mules)} | Legit: {len(legit)}")

## YOUR TASK — LABEL & MULE FLAG ANALYSIS

1. MULE FLAG DATE DISTRIBUTION
   - Plot mule_flag_date by month (bar chart)
   - Which months had the most flagging activity?
   - Is flagging accelerating over time or steady?
   - Save as: reports/step3_eda_plots/01_mule_flag_dates.png

2. ALERT REASON ANALYSIS
   - Value counts of alert_reason column
   - What are the top 5 alert reasons?
   - Plot as horizontal bar chart
   - Save as: reports/step3_eda_plots/02_alert_reasons.png

3. FLAGGED BY BRANCH ANALYSIS
   - How many unique branches flagged mules?
   - Top 10 branches by mule count
   - Are any branches responsible for a disproportionate share?
     (If top branch > 10% of all flags → 🔍 SIGNAL)
   - Plot top 20 branches
   - Save as: reports/step3_eda_plots/03_flagging_branches.png

4. MULE ACCOUNT AGE AT FLAGGING
   - Compute: mule_flag_date - account_opening_date = age at flag
   - Distribution: min, max, mean, median
   - Plot histogram of age at flagging (in days)
   - How many were flagged within:
       First 90 days
       90–365 days
       1–2 years
       2+ years
   - Save as: reports/step3_eda_plots/04_age_at_flagging.png

## OUTPUT
- Print all findings with section headers
- Save all 4 plots
- End with: === TASK 3A COMPLETE ===