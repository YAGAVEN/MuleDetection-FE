You are a data analysis agent for a financial crime detection hackathon.
master and transactions dataframes are loaded.
train_labels are merged into master as is_mule column.

## YOUR TASK — TRANSACTION VOLUME ANALYSIS

First, build a transaction summary per account:
txn_summary = transactions.groupby('account_id').agg(
    txn_count        = ('transaction_id', 'count'),
    total_credit     = ('amount', lambda x: x[x > 0].sum()),
    total_debit      = ('amount', lambda x: x[x < 0].sum()),
    avg_amount       = ('amount', 'mean'),
    max_amount       = ('amount', 'max'),
    min_amount       = ('amount', 'min'),
    unique_counterparties = ('counterparty_id', 'nunique'),
    first_txn        = ('transaction_timestamp', 'min'),
    last_txn         = ('transaction_timestamp', 'max'),
).reset_index()

txn_summary['active_days'] = (
    txn_summary['last_txn'] - txn_summary['first_txn']
).dt.days

txn_summary = txn_summary.merge(
    master[['account_id', 'is_mule', 'account_opening_date']],
    on='account_id', how='left'
)

## ANALYSES

1. TRANSACTION COUNT DISTRIBUTION
   - Compare txn_count: mules vs legit
   - Mean, median, 90th percentile for each
   - Plot histogram (log scale x-axis)
   - Save as: reports/step3_eda_plots/16_txn_count.png

2. CREDIT vs DEBIT VOLUME
   - Compare total_credit and total_debit: mules vs legit
   - Net flow = total_credit + total_debit (debit is negative)
   - Net flow distribution: mules vs legit
   - Plot boxplot (cap at 99th percentile)
   - Save as: reports/step3_eda_plots/17_credit_debit_volume.png

3. COUNTERPARTY DIVERSITY
   - Compare unique_counterparties: mules vs legit
   - Ratio: unique_counterparties / txn_count
   - High ratio = many different parties (fan-in/fan-out signal)
   - Plot distribution
   - Save as: reports/step3_eda_plots/18_counterparty_diversity.png

4. TRANSACTION ACTIVITY WINDOW
   - Compare active_days: mules vs legit
   - Accounts active < 30 days vs 30–180 days vs 180+ days
   - Plot histogram
   - Save as: reports/step3_eda_plots/19_active_days.png

5. TEMPORAL PATTERNS
   - Transaction count by month (Jul 2020 – Jun 2025)
   - Separate lines for mule accounts vs legit accounts
   - Any spikes in specific months?
   - Transaction count by day of week: mules vs legit
   - Transaction count by hour of day: mules vs legit
   - Save as: reports/step3_eda_plots/20_temporal_patterns.png

## OUTPUT
- Print all findings with section headers
- Save all 5 plots
- Flag signals as 🔍 SIGNAL
- End with: === TASK 3D COMPLETE ===