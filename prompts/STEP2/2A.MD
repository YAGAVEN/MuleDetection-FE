You are a data analysis agent for a financial crime detection hackathon.

## SETUP — Run this first
import pandas as pd
import numpy as np

master = pd.read_csv('Mule-data/master.csv', 
         dtype={'account_id': str, 'customer_id': str})
transactions = pd.read_csv('Mule-data/transactions_full.csv',
               dtype={'account_id': str, 'transaction_id': str,
               'counterparty_id': str},
               parse_dates=['transaction_timestamp'])

print("master shape:", master.shape)
print("transactions shape:", transactions.shape)

## YOUR TASK — NULL / MISSING VALUE ANALYSIS

1. Compute null count and null % for every column in master
2. Group columns into:
   A) Zero nulls
   B) Expected nulls (freeze_date when not frozen, mule_flag_date 
      for legitimate accounts) — these are acceptable
   C) Unexpected nulls — flag as WARNING

3. For every column that has nulls, compare null rate between:
   - mule accounts (is_mule == 1)
   - legitimate accounts (is_mule == 0)
   Print side by side.

4. For transactions, check nulls in these critical columns only:
   transaction_id, account_id, amount, transaction_timestamp, txn_type
   Any null here = WARNING

## OUTPUT
- Print results clearly with section headers
- End with: === TASK 1 COMPLETE ===
- Do NOT save any files yet